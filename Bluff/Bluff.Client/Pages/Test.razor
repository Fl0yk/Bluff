@page "/test"

<style>
    table {
        border-collapse: collapse;
    }

    td {
        width: 100px;
        height: 100px;
        border: 2px solid purple;
        text-align: center;
        vertical-align: middle;
    }

    td.empty{
            border: none;
    }
</style>

<h3>Игровое поле</h3>

@{
    Board board = new();
    // Создаем двумерный массив для хранения объектов Cell
    Cell[,] cells = board.ToMatrix();

    // Заполняем только первую и последнюю строку, а также левый и правый столбец
    // for (int row = 0; row < 7; row++)
    // {
    //     for (int col = 0; col < 10; col++)
    //     {
    //         bool isFirstOrLastRow = row == 0 || row == 6;
    //         bool isLeftOrRightColumn = col == 0 || col == 9;

    //         if (isFirstOrLastRow || isLeftOrRightColumn)
    //         {
    //             cells[row, col] = new Cell
    //                     {
    //                         Position = (row, col),
    //                         Id = (row * 10) + col + 1,
    //                         Value = (row * 10) + col + 1
    //                     };
    //         }
    //     }
    // }
}

<table>
    @for (int row = 0; row < 7; row++)
    {
        <tr>
            @for (int col = 0; col < 10; col++)
            {
                //<td>@(cells[row, col]?.Value ?? 0)</td>
                @if (cells[row, col] is null || cells[row, col].Value == 0)
                {
                    <td class="empty"></td>
                }
                else if (cells[row, col].IsStar)
                {
                    <td>*<br/>@(cells[row, col]?.Value.ToString())</td>
                }
                else
                {
                    <td>@(cells[row, col]?.Value.ToString())</td>
                }
            }
        </tr>
    }
</table>